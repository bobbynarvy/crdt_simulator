defmodule CRDT.ClusterManagerTest do
  use ExUnit.Case
  import CRDT.ClusterManager
  doctest CRDT.ClusterManager

  test "initializes replicas" do
  end

  test "receives events from replicas" do
  end

  test "propagates events to replicas" do
    # asynchronously
  end
end
